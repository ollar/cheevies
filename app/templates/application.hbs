{{#if session.isAuthenticated}}
{{site-header}}
{{/if}}

{{#ember-notify as |message close|}}
  <div class="notification is-{{message.type}}">
    <button class="delete" {{action close}}></button>
    {{message.text}}
  </div>
{{/ember-notify}}

<main class="{{routeStyleNamespaceClassSet}}">
    {{outlet}}
</main>

{{site-footer}}
