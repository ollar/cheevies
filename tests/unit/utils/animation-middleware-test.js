import animationMiddleware from 'cheevies-jerk/utils/animation-middleware';
import { module, test } from 'qunit';

module('Unit | Utility | animation middleware');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = animationMiddleware();
  assert.ok(result);
});
