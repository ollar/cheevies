import imageResize from 'cheevies-jerk/utils/image-resize';
import { module, test } from 'qunit';

module('Unit | Utility | image resize');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = imageResize();
  assert.ok(result);
});
