// import DS from 'ember-data';
export function initialize(application) {
  console.log(2, application);

  // application.register('adapter:application', DS.RESTAdapter);
}
export default {
  initialize,
};
