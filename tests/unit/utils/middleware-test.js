import middleware from 'cheevies-jerk/utils/middleware';
import { module, test } from 'qunit';

module('Unit | Utility | middleware', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = middleware();
    assert.ok(result);
  });
});
