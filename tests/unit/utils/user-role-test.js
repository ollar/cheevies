import userRole from 'cheevies-jerk/utils/user-role';
import { module, test } from 'qunit';

module('Unit | Utility | user-role', function() {
    // Replace this with your real tests.
    test('it works', function(assert) {
        let result = userRole();
        assert.ok(result);
    });
});
